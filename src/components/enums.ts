export enum EngineStatus {
    Start = 'started',
    Stop = 'stopped'
}
export enum SortQuery {
    id = 'id',
    wins = 'wins',
    time = 'time',
}
export enum OrderQuery {
    ASC = 'ASC',
    DESC = 'DESC'
}
